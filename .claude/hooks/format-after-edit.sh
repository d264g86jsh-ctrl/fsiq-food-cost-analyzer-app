#!/bin/bash
# Hook: format-after-edit
# Placeholder — runs after Edit/Write tool calls.
# TODO: add formatting/lint check here when desired, e.g.:
#   pnpm lint --fix $CLAUDE_FILE_PATH
# Do not run automatically until configured.
exit 0

#!/bin/bash
# Hook: block-dangerous-commands
# Placeholder — inspect Bash tool calls before they run.
# TODO: add patterns to block (e.g. rm -rf /, git push --force origin main).
# Do not add logic until configured.
exit 0

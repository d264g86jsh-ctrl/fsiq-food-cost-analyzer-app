import { describe, it, expect } from "vitest";

// Placeholder test — confirms pnpm test works.
// Phase 1+ will add real business logic tests.
describe("scaffold", () => {
  it("test runner is working", () => {
    expect(1 + 1).toBe(2);
  });
});
